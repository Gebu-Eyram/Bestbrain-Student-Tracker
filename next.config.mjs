/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "github.com",
      "api.dicebear.com",
      "images.unsplash.com",
      "assets.aceternity.com",
    ],
  },
};

export default nextConfig;
